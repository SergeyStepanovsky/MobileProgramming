<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/textViewQuestion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Is your number X?"
        android:textSize="24sp"/>

    <Button
        android:id="@+id/buttonHigher"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Higher"
        android:onClick="onHigher"/>

    <Button
        android:id="@+id/buttonLower"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Lower"
        android:onClick="onLower"/>

    <!-- Кнопка для подтверждения правильного числа -->
    <Button
        android:id="@+id/buttonCorrect"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Correct"
        android:onClick="onCorrect"/>
</LinearLayout>
